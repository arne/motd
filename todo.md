# TODO

- [ ] `motd doctor` — show which modules are stale, missing, or slow
- [ ] Shell hook — add `motd` to shell rc; deferred until happy with the output

## Done — 2026-04-30

- [x] More whimsical mark for cube — replaced wireframe with chafa-rendered colored ASCII; ultimately abandoned cube concept for a 37-animal library, default elephant
- [x] Polish hstack gap / vstack section-break spacing — accepted as-is
- [x] `--watch` mode for iterating on the YAML (fsnotify, clear screen + rerender on save)
